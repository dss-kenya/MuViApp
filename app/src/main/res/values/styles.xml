<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextStyleWhite" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/txt_large</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textColorHint">@color/colorGrey</item>
        <item name="android:padding">@dimen/margin_mid_large</item>
    </style>

    <style name="ButtonStyle" parent="TextStyleWhite">
        <item name="android:textSize">@dimen/txt_large</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:background">@drawable/button_selector</item>
    </style>

    <style name="TextStyleWhite.Small" parent="TextStyleWhite">
        <item name="android:textSize">@dimen/txt_small</item>
        <item name="android:textColor">@color/colorGreen</item>
    </style>

</resources>
